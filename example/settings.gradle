pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        maven { url 'https://developer.huawei.com/repo/' }
        mavenCentral()
        mavenLocal()
    }
}

rootProject.name = "Mindbox Android Sdk Example"
include ':app'
include ':mylibrary'
